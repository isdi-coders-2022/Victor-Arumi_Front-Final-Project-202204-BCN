import styled from "styled-components";

const NavBarStyled = styled.div`
  min-width: 350px;
  box-shadow: 0px 8px 20px -5px rgba(0, 0, 0, 0.47);

  .logo {
    margin-left: 40px;
  }
`;

export default NavBarStyled;
